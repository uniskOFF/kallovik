/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
  basePath: '/kallovik',
  assetPrefix: '/kallovik/',
}

export default nextConfig